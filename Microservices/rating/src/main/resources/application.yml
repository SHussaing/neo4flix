spring:
  application:
    name: RATING-SERVICE



server:
  port: 8083
  ssl:
    enabled: false


# Eureka (Discovery)
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
